IMPORTANTE: Primero crear dos roles, uno llamado "client".

1. Establecer estructura de la base de datos
REGISTRO (dos)
Colección Clients {
  _id: ObjectId,
  firstName: String,
  lastName: String,
  email: String,
  password: String,
  roleId: idRole,
  address: [
    {
      country: String,
      city: String
    }
  ],
  nit: String,
  dbStatus: Boolean,
  registerDate: Date
}

FACTURA (tres)
Colección invoices {
  _id: ObjectId,
  invoiceCode: String,
  invoiceTotal: [
    {
      subTotal: Number,
      iva: Number
    }
  ],
  invoiceDate: Date,
  invoiceStatus: String,
  paid: Boolean,
  paidDate: Date,
  userId: idUser
}

2. Crear esquema y modelo de la base de datos
3. Crear controladores de la base de datos
3.1 Clientes -> Colección, usuarios
3.1.1 Función (post) registro de clientes
3.1.2 Función (get) lista de clientes
4. Crear vinculación del servidor con la base de datos
5. Crear las rutas de conexión
6. Crear la ruta de conexión del backend con el frontend y probar el funcionamiento correcto de la base de datos
7. Crear las autorizaciones y validaciones de los registros e ingresos de los usuarios